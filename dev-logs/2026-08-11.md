# Dev Log — 2026-08-11

## 完成事项

### v1.4.3 资产架构重构（7 阶段）

**Phase 1: 数据库迁移 v11**
- `investment_accounts` 新增 `cash_balance` 字段
- `account_transactions` 新增 `investment_account_id` 字段
- 新建 `fixed_deposits` 表（定期存款）

**Phase 2: 基金价格获取**
- 新增 `fetchFundPrice()` — 对接天天基金 API (`fundgz.1234567.com.cn`)
- `fetchAllPrices()` 路由修正：基金不再走 `detectMarket()` → stock API

**Phase 3: 持仓关联投资账户**
- `AddAssetForm.tsx` 新增投资账户下拉框
- `HoldingsDetail.tsx` 编辑弹窗支持更改投资账户关联

**Phase 4: 银行父账户汇总余额**
- `Accounts.tsx` 新增 `getTotalBalanceRecursive()` 递归求和函数
- 父账户余额显示包含所有子账户 + 关联投资账户市值

**Phase 5: 证券账户现金余额**
- `investment-account-service.ts` 新增 `cash_balance` 支持 + `addCashBalance()` / `withdrawCashBalance()`
- `account-transaction-service.ts` 取款时自动追踪转入投资账户的现金
- `AccountDetail.tsx` 取款表单新增「转入投资账户」下拉框
- `Investments.tsx` 投资账户卡片显示闲置现金

**Phase 6: 定期存款 CRUD**
- 新建 `fixed-deposit-service.ts` — 完整 CRUD + 银行余额自动调整
- IPC 注册 4 个 `fixedDeposit:*` handler
- `AccountDetail.tsx` 新增定期存款卡片 + 添加/编辑/删除 Modal

**Phase 7: 资产结构分类修正**
- `getAllAssetsSummary()` 中定期存款金额从现金分类扣除，归入投资分类

### 版本/构建
- 版本号：`1.4.2` → `1.4.3`
- 构建：Vite + tsc 零错误通过

## 待办
- 打包 v1.4.3 安装包并发布到 GitHub
- 后续可考虑：定存到期提醒、基金净值历史图表

## 下一步
- 打包发布 v1.4.3
