# 2026-08-16 开发日志

## 完成事项

### v1.6.1 总资产口径修复（银行组内嵌券商漏计 + 净值历史窗口）

1. **问题复现（端到端实测，非猜测）**
   - 用真实编译产物 + 演示数据复现：顶部总资产卡 ¥487,950（正确 ¥713,350），投资市值卡 ¥178,400（正确 ¥403,800），净资产快照同样少 ¥225,400——恰为银行组内嵌券商「富途证券」市值
   - 根因：银行组的内嵌券商从「现金及存款」扣除后未加回「投资市值」，Dashboard 与 recordNetWorth 两处各自手写同一口径、同步漂移

2. **单一口径源（防再次漂移）**
   - 新增 `src/shared/utils/asset-totals.ts`：`computeAssetTotals(items)` 纯函数（现金及存款 / 券商流动金 / 投资市值 / 总资产）
   - `Dashboard.tsx` 与净资产记录共用该函数

3. **净资产层拆分（可测试）**
   - 新增 `src/main/database/services/net-worth-core.ts`：`recordNetWorthInDb(db, items)` + `getNetWorthHistoryInDb(db, days)`（纯 DB，无 electron 依赖）
   - `net-worth-service.ts` 改为薄封装
   - 修复历史窗口 bug：`ORDER BY date ASC LIMIT ?` 取的是最早 N 天 → 改为 DESC LIMIT 再升序返回（最近 N 天）

4. **测试**
   - 新增 `tests/unit/asset-totals.test.ts`（6 个用例：演示数据口径/内嵌券商回归/顶级和恒等/空数组/null 值/多层嵌套）
   - 新增 `tests/integration/net-worth-core.test.ts`（4 个用例：快照口径/upsert/最近30天窗口/不足N天）
   - 全量 71/71 通过；两处 tsc 无错；IPC 白名单 145 频道三处一致

5. **端到端验证脚本**
   - 新增 `scripts/verify-demo-totals.js`：灌演示数据 → stub electron 加载真实 dist 服务 → 逐项打印并比对预期（总资产 ¥713,350），可重复执行
   - 本次运行全部通过（含 40 天历史预插验证「最近 N 天」窗口）

6. **文档同步**
   - `package.json`：1.6.0 → 1.6.1
   - `docs/tech-spec.md`：数据服务 17→20（asset-cny-core / cash-flow-core / net-worth-core 补入树），shared/utils 增 asset-totals.ts
   - `docs/requirements.md`：v1.6.1 条目补充内嵌券商/历史窗口/单一口径说明
   - `docs/dev-roadmap.md`：第 29 阶段更新（代码+验证完成，待打包发布）

### v1.6.1 汇率刷新后页面数据分叉修复（资产管理页 676,570.38 vs 总览 713,350）

1. **诊断（对用户演示库只读实查）**
   - 新增 `scripts/inspect-summary.js`：对指定库直接调用真实 dist 服务打印总览口径，全程只读
   - 实锤：用户演示库总资产 = 676,570.38，与资产管理页显示完全一致——资产管理页没错；总览页 713,350 是过期数据
   - 根因：应用启动时 `startScheduler()` 立即抓取实时汇率覆盖 `currencies` 表（种子 HKD 0.92/USD 7.25 → 实时 0.862069/6.756757，数值经交叉验证为真实汇率），但主进程不通知渲染端，页面只在挂载时拉一次数据 → 两页分叉

2. **修复（方案 A）**
   - `exchange-rate-fetcher.ts`：`broadcastCurrencyUpdated()` 在汇率更新成功后向所有窗口发 `currency:updated` 事件（覆盖启动抓取/每 6 小时/手动更新三处）
   - `preload.ts`：新增 `onCurrencyUpdated` / `removeCurrencyUpdatedListener`；`vite-env.d.ts` 同步类型
   - 新增 `src/renderer/hooks/useCurrencyRefresh.ts`；Dashboard / Accounts / Investments 三页接入，汇率一变自动重载

3. **演示验证口径修正（方案 B）**
   - `seed-demo-data.js` 预期金额输出注明：基于种子汇率、启动后会被实时汇率覆盖，不以固定值 ¥713,350 作锚点
   - 验证改为一致性核对：启动应用 → `node scripts/inspect-summary.js <库路径>` → 四个位置应全部等于脚本输出的顶级项合计

4. **验证**：两处 tsc 无错；71/71 测试通过；IPC 白名单 145 频道一致；verify-demo-totals 回归全过；双端构建成功

5. **文档同步**：tech-spec（hooks 树）、requirements（v1.6.1 条目）、dev-roadmap（第 29 阶段）

### v1.6.1 银行账户「取出资金」报错修复

1. **问题**：银行账户取出报 `参数校验失败（accountTransaction:create）：0.investment_account_id：Too small: expected number to be >0`；任何银行取出均失败，存入正常
2. **根因**：取出时表单渲染「转入投资账户（可选）」下拉（存在券商账户即渲染），默认「不转入」提交空串 `""`；schema `investment_account_id: id.nullish()`（id 为 coerce positive）把空串强转成 0 → positive 校验失败；存入不渲染该字段所以不受影响；服务层本就兼容 0（`|| null`）
3. **修复**：
   - `AccountTransactionsSection.tsx`：提交前归一化——空串/0/null 时删除该字段，否则 parseInt 转数字
   - `ipc-validation.ts`：`investment_account_id` 改为 `z.coerce.number().int().nonnegative().nullish().transform(v => (v && v > 0 ? v : null))`（handleValidated 以解析结果替换入参，主进程收到干净 null；负数/非数字仍拒绝）
   - `tests/unit/validation.test.ts`：新增 1 个用例覆盖空串/0/缺省→null、正数保留、负数/非数字拒绝
4. **验证**：两处 tsc 无错；72/72 测试通过；IPC 白名单 145 频道一致；双端构建成功
5. **文档**：requirements / dev-roadmap 第 29 阶段已同步

### v1.6.1 资产间联动全部询问式（用户确认后改造）

1. **审计**：梳理全部 12 处资产间联动——定存创建（已有询问但无记录）、定存编辑/删除（自动不询问）、到期回款（完全缺失）、存取记录编辑（券商现金不同步）、保费缴纳（选账户即扣）等。
2. **定存全链路询问式**：
   - `fixed-deposit-core.ts`（纯 DB，可测试）+ service 薄封装：创建扣款同时写存取记录；编辑 balanceMode=sync/record_only（差额调整写记录 / 脱钩转纯记录型）；删除 restoreBalance=true/false（退回写记录 / 仅删）；新增 `settleFixedDepositInDb` 到期回款（写存款记录 + status=settled，幂等）
   - 迁移 v17：`fixed_deposits.status`（active/settled，存量默认 active）
   - IPC：`fixedDeposit:update` 第三参 balanceMode、`fixedDeposit:delete` 第二参 restoreBalance、新频道 `fixedDeposit:settle`（schema/类型联合/preload 白名单同步，146 频道一致）
   - 渲染端 `FixedDepositsSection`：编辑差额询问弹窗（同步调整并写记录 / 仅改记录转纯记录型）、删除询问弹窗（退回并写记录 / 仅删记录）、到期处理弹窗（回款金额默认本金+利息可改 + 回款账户）、列表显示 ✅ 已结算
3. **存取记录联动**：`updateAccountTransaction` 增加 syncBrokerCash 参数（撤销旧联动 + 应用新联动 / 拒绝则脱钩置空）；编辑弹窗带券商联动时显示同步复选框；删除弹窗明示将同步扣回券商流动金。
4. **保费缴纳**：提示语明确「选账户即扣款并写记录 / 不指定仅记录不动余额」。
5. **验证**：两处 tsc 无错；79/79 测试通过（新增 fixed-deposit 6 用例 + v17 迁移 1 用例）；IPC 146 频道一致；双端构建成功。
6. **文档**：data-model（v17 + status 字段）、tech-spec（服务 21、fixedDeposit 频道行、白名单 146）、requirements（联动询问式全链路）、dev-roadmap（第 29 阶段）。

### v1.6.1 打包发布 ✅

- 用户 GUI 验证通过后执行发布：`npm run release:local` 生成 `release/personal-finance-setup-1.6.1.exe`（114.83 MB）+ blockmap + latest.yml
- `scripts/upload-release.js` 上传 GitHub Releases：https://github.com/Davidtsoi06/personal-finance-app/releases/tag/v1.6.1
- 代码与标签已推送 GitHub（master 4f25339）
- `RELEASE_NOTES.md` 更新为 v1.6.1 说明；dev-roadmap 第 29 阶段标记已发布

### v1.6.1 补丁：无法添加券商账户修复

1. **问题**：添加投资账户（不选关联银行）静默失败——表单「无关联」提交空串，`funding_account_id: id.nullish()`（coerce positive）把空串强转 0 → 校验拒绝；handleAdd 的 catch 仅 console.error，界面无任何提示
2. **修复**：`Investments.tsx` handleAdd 归一化（空串转 null / 删除空 funding_account_id）+ 弹窗显示错误信息；`ipc-validation.ts` funding_account_id 改为 coerce nonnegative nullish + transform 转 null（与 investment_account_id 同款，update 同步生效）
3. **测试**：validation.test.ts 新增 create/update 各分支用例（空串/0/缺省→null、正数保留、负数/非数字拒绝），80/80 通过；tsc 无错；IPC 146 频道一致；双端已重建

### v1.7.0 启动密码锁（新功能，用户确认方案后实现）

1. **设计**：自定义密码（≥6 位，scrypt 随机盐哈希）；忘记密码走邮箱验证码（6 位/10 分钟/60 秒限发/5 次试错作废）；启动验证 + 空闲 10 分钟自动锁（1~60 可调）+ 手动锁定；登录失败 5 次锁 30 秒
2. **实现**：
   - `auth-core.ts`（纯逻辑：哈希/验证码/登录与发信限流/邮箱脱敏）+ `auth-service.ts`（门禁状态、SMTP 配置、nodemailer 发信、验证码重置流程）
   - 独立锁屏窗口 + 最小权限 `lock-preload.ts`（仅 auth 频道）；`main/index.ts` 启动门禁（启用密码则先锁屏，验证通过才建主窗）；`validation.ts` 全局门禁（未解锁拒绝业务 IPC）
   - 渲染端：`LockScreen`（登录/忘记密码三步流程，#/lock 独立渲染不经 Layout）、`SecurityCard`（设置页：SMTP/恢复邮箱/测试发信/密码管理/空闲时长/立即锁定）、`useIdleLock` 空闲自动锁
   - IPC 新增 14 个 `auth:*` 频道（schema/类型联合/白名单三处一致，146→160）；SMTP 授权码用既有 AES-GCM（crypto-util）加密存储；依赖 +nodemailer
3. **测试**：auth-core 7 用例（哈希/盐随机/篡改/验证码/登录限流/发信限流/脱敏）+ auth schema 校验；88/88 通过；tsc 无错
4. **文档**：requirements（新功能）、tech-spec（服务 14、页面 13、频道表、白名单 160、hooks）、data-model（app_settings 键）、design-spec（锁屏页/安全卡片）、roadmap 第 30 阶段；版本 1.7.0

## 待办

- [ ] 用户 GUI 验证：启用密码（SMTP+测试邮件）、重启验证锁屏、空闲/手动锁定、忘记密码邮箱找回
- [ ] 验证通过后打包发布 v1.7.0

## 问题

- 用户粘贴的截图 modlens 读取失败（`spawn EINVAL`），已请用户运行 `npx @liustack/modlens doctor` 检查；本轮以端到端脚本复现代替截图定位问题

## 下一步

- 用户验证通过后执行发布流程（`npm run release:local` + GitHub Releases 上传）