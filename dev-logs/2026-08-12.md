# 2026-08-12 开发日志

## 完成事项

### v1.5.0 四层资产架构重构

1. **数据库迁移 v12**
   - 新增 `insurance_policies` 表（保单管理）
   - 新增 `premium_payments` 表（保费记录）
   - `accounts` 表新增 `display_alias` 字段
   - JS 迁移函数支持（`Migration.migrate(db)`）
   - 迁移逻辑：保险 accounts → policies、删除信用卡、子账户提升、空父账户删除、系统钱包自动创建

2. **服务层新增/修改**
   - `insurance-service.ts`：保单 CRUD + 保费缴纳（事务：保费记录 + 银行扣款 + 流水账）
   - `account-service.ts`：四层架构 `getAllAssetsSummary()`、`listByBankName()`、`listBankAccounts()`
   - `account-transaction-service.ts`：`importWalletBills()` 双写（transactions + ledgers）
   - `ledger-service.ts`：`listLedgers()` 支持 `accountId` 过滤
   - `AccountRow` 新增 `display_alias` 字段

3. **IPC 层新增**
   - `insurance-ipc.ts`：9 个频道（保单 CRUD + 保费 + 查询）
   - `wallet-ipc.ts`：4 个频道（系统钱包 + 账单导入 + 银行列表）
   - `asset-ipc.ts`：新增 `asset:listByAccount` 频道

4. **前端页面**
   - **Accounts.tsx 完全重写**：Layer 2 卡片布局 + 统计卡片 + 银行分组可展开
   - **WalletFlow.tsx 新建**：钱包流水页（/wallet/:type）
   - **Insurance.tsx 新建**：保单管理页（/insurance）
   - **AccountDetail.tsx 增强**：银行理财产品卡片
   - **HoldingsDetail.tsx**：交易记录改为 SlidePanel 侧边滑出
   - **Dashboard.tsx**：饼图新增 e_wallet 分类（图标 + 颜色）
   - **App.tsx**：新增 /wallet/:type 和 /insurance 路由

5. **SlidePanel 组件**
   - 新建 `SlidePanel.tsx` + `SlidePanel.css`
   - 右侧滑入动画、Escape 关闭、点击遮罩关闭

6. **定时任务**
   - `scheduler.ts`：新增 `schedulePremiumDueCheck()` 每日 8:57 检查到期保单

7. **文档同步更新**
   - `package.json`：版本 v1.4.3 → v1.5.0
   - `docs/dev-roadmap.md`：新增 v1.5.0 阶段
   - `docs/tech-spec.md`：IPC 5→8、服务 15→17、页面 10→12、组件 7→8
   - `docs/data-model.md`：表 17→19、v12 迁移、insurance_policies + premium_payments
   - `docs/requirements.md`：F2 重写、新增 F21/F22
   - `RELEASE_NOTES.md`：v1.5.0 更新说明

### 构建验证
- `npm run build` 零错误通过

## 下一步
- 打包发布 Windows 安装包（electron-builder）
- 测试实际运行效果

---

### v1.5.1 基金价格 API 紧急修复

8. **问题诊断**
   - 天天基金 `fundgz.1234567.com.cn` 返回 HTML 错误页面（防盗链升级）
   - 东方财富 `fundgzapi.eastmoney.com` 返回 302 重定向到 HTML
   - 两个基金价格数据源全部失效

9. **修复**
   - 主源 → 东方财富历史净值 API：`api.fund.eastmoney.com/f10/lsjz`（JSON，含 DWJZ/LJJZ/FSRQ）
   - 备用源 → 新浪财经基金 API：`hq.sinajs.cn/list=f_XXXXXX`（与 A 股同体系）
   - 两个新源已验证 5 只基金均返回正确数据

10. **文档同步**
    - `docs/api-reference.md`：更新第 5 节基金 API，替换旧源为新源
    - `RELEASE_NOTES.md`：新增 v1.5.1 更新说明
    - `package.json`：版本 1.5.0 → 1.5.1

11. **构建 + 发布**
    - `personal-finance-setup-1.5.1.exe`（112 MB）打包成功
    - 已删除旧安装包 `personal-finance-setup-1.5.0.exe`
    - 已推送 GitHub（`8d4ad7f`）
