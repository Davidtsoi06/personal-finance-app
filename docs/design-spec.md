# 设计规范 — 个人理财投资软件

## 设计原则

- **简洁**：减少视觉噪音，聚焦核心信息
- **直观**：信息层级清晰，操作符合直觉
- **一致**：相同元素使用相同的样式和行为
- **可读**：字号、对比度确保财务数据的清晰可读

---

## 色彩系统

### 主色调 — 淡蓝色系

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-primary` | `#5B9BD5` | 主色调 |
| `--color-primary-50` | `#EBF3FB` | 浅色背景 |
| `--color-primary-100` | `#D6E7F7` | 悬停背景 |
| `--color-primary-300` | `#8BBDE4` | 边框、次要图标 |
| `--color-primary-500` | `#5B9BD5` | 主色、按钮、链接 |
| `--color-primary-700` | `#3A7CC3` | 深色主色、按下状态 |
| `--color-primary-900` | `#1F5A9A` | 最深主色、标题栏背景 |

### 辅助色

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-success` | `#52C41A` | 盈利、收入、正数 |
| `--color-danger` | `#FF4D4F` | 亏损、支出、负数、警告 |
| `--color-warning` | `#FAAD14` | 提醒、待处理 |
| `--color-info` | `#1890FF` | 信息提示 |

### 中性色

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-bg` | `#F5F7FA` | 页面背景 |
| `--color-bg-secondary` | `#FAFBFC` | 次级背景（表头等） |
| `--color-surface` | `#FFFFFF` | 卡片/面板背景 |
| `--color-border` | `#E4E7ED` | 边框 |
| `--color-text` | `#1F2937` | 主要文字 |
| `--color-text-secondary` | `#6B7280` | 次要文字 |
| `--color-text-muted` | `#9CA3AF` | 辅助文字 |

---

## 字体

| Token | 值 | 用途 |
|-------|-----|------|
| `--font-family` | `"Microsoft YaHei", "PingFang SC", sans-serif` | 正文字体 |
| `--font-family-number` | `"Consolas", "Menlo", monospace` | 数字/金额字体 |
| `--font-size-xs` | `12px` | 标签、辅助文字 |
| `--font-size-sm` | `14px` | 正文、表格内容 |
| `--font-size-md` | `16px` | 标题、导航 |
| `--font-size-lg` | `20px` | 页面标题 |
| `--font-size-xl` | `28px` | 金额大数字 |
| `--font-size-xxl` | `36px` | 仪表盘核心数字 |

---

## 间距

采用 4px 基准间距，递增为 4 的倍数：

| Token | 值 |
|-------|-----|
| `--spacing-xs` | `4px` |
| `--spacing-sm` | `8px` |
| `--spacing-md` | `16px` |
| `--spacing-lg` | `24px` |
| `--spacing-xl` | `32px` |
| `--spacing-xxl` | `48px` |

---

## 圆角

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | `4px` | 输入框、按钮、标签 |
| `--radius-md` | `8px` | 卡片 |
| `--radius-lg` | `12px` | 大卡片、模态框 |

---

## 阴影

| Token | 值 | 用途 |
|-------|-----|------|
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.05)` | 卡片默认 |
| `--shadow-md` | `0 4px 12px rgba(0,0,0,0.08)` | 卡片悬停 |
| `--shadow-lg` | `0 8px 24px rgba(0,0,0,0.12)` | 模态框、下拉菜单 |

---

## 布局规范

```
┌──────────────────────────────────────────┐
│  侧边栏 (220px)    │    内容区域          │
│  ┌──────────────┐  │                     │
│  │  Logo / 标题  │  │  ┌───────────────┐  │
│  │              │  │  │  页面标题       │  │
│  │  导航菜单     │  │  ├───────────────┤  │
│  │  📊 仪表盘   │  │  │               │  │
│  │  💳 账户管理 │  │  │  页面内容      │  │
│  │  📈 投资管理 │  │  │               │  │
│  │  🤖 AI 助手  │  │  │               │  │
│  │  📝 记账     │  │  │               │  │
│  │  📉 报表分析 │  │  │               │  │
│  │  ⚙️ 设置     │  │  │               │  │
│  │              │  │  └───────────────┘  │
│  └──────────────┘  │                     │
└──────────────────────────────────────────┘
```

- 侧边导航：固定 220px 宽
- 内容区域：自适应宽度，内边距 `spacing-xl`
- 最小窗口：1280 × 720

---

## 组件规范

### 按钮（Button）

| 变体 | 样式 |
|------|------|
| `primary` | `primary-500` 底色，白色文字 |
| `secondary` | 白底，`primary-500` 边框和文字 |
| `danger` | `danger` 底色，白色文字 |
| 大小 | `sm`（32px）/ 默认（40px） |
| 加载态 | 文字变为 `⏳ xxx中...`，按钮禁用 |

### 输入框（Input）

- 边框 `--color-border`，聚焦时 `--color-primary-500`
- 高度 `40px`
- 圆角 `--radius-sm`
- placeholder 颜色 `--color-text-muted`

### 卡片（Card）

- 白底，`--radius-md` 圆角，`--shadow-sm` 阴影
- 标题 + 内容结构
- 内边距 `--spacing-lg`
- 页面上卡片间距 `--spacing-lg`

### 表格（Table）

- 表头背景 `--color-bg-secondary`
- 行悬停背景 `--color-primary-50`
- 金额列右对齐，使用数字字体
- 空数据显示 "暂无数据"

### 模态框（Modal）

- 居中弹出，`--shadow-lg` 阴影
- 半透明黑色遮罩（`rgba(0,0,0,0.45)`）
- 标题 + 内容 + 操作按钮区
- 点击遮罩可关闭

### 金额显示（Amount）

- 正值：`--color-success`，前缀 `+`
- 负值：`--color-danger`，前缀 `-`
- 币种符号在前面
- 保留两位小数
- 使用 `--font-family-number`

### 进度条（ProgressBar）

颜色根据百分比自适应：

| 范围 | 颜色 | 含义 |
|------|------|------|
| 0–60% | `--color-success`（绿） | 安全 |
| 60–80% | `--color-primary`（蓝） | 正常 |
| 80–100% | `--color-warning`（橙） | 预警 |
| >100% | `--color-danger`（红） | 超支 |

### 标签徽章（Badge）

- 圆角小标签
- 支持颜色变体：success / danger / warning / info / default

### 统计卡片（Dashboard stat cards）

- 大数字（`--font-size-xxl`）+ 标签文字
- 四列网格布局（`grid-template-columns: repeat(4, 1fr)`）
- 响应式：窄屏自动变两列

---

## 页面布局

### 仪表盘 (Dashboard)

```
┌─────────────────────────────────────────┐
│  仪表盘  ·  资产总览                     │
├─────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │总资产 │ │现金  │ │投资  │ │月收支 │  │
│  └──────┘ └──────┘ └──────┘ └──────┘  │
│                                         │
│  ┌─────────────┐ ┌──────────────┐      │
│  │ 资产分布饼图 │ │ 账户概览     │      │
│  └─────────────┘ └──────────────┘      │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 📈 总资产走势 (30天)             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 📊 本月预算 · 进度条 · 剩余/天   │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### AI 助手 (AIAssistant)

```
┌─────────────────────────────────────────┐
│  🤖 AI 投资助手                         │
│  基于真实持仓数据，提供投资分析和建议     │
├─────────────────────────────────────────┤
│  [📊分析组合] [⚠️风险评估] [💡优化建议]  │
│  [📝投资报告] [🔍消费分析]              │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  👤 分析一下我的投资组合         │   │
│  │                                 │   │
│  │  ┌─────────────────────────┐    │   │
│  │  │ 🤖 AI 回复（Markdown）   │    │   │
│  │  │ ## 📊 投资组合分析       │    │   │
│  │  │ ...                     │    │   │
│  │  └─────────────────────────┘    │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [展开数据快照 ▾]    提供者: DeepSeek    │
│  ┌─────────────────────────────────┐   │
│  │ 💬 输入问题...              📤  │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 样式方法论

- **BEM 命名**：组件级使用 BEM（Block__Element--Modifier）
  - 如：`.budget-card__alert--warning`、`.stat-card__label`
- **CSS 文件**：每个页面/组件对应一个 CSS 文件
- **设计令牌**：所有颜色/字体/间距使用 CSS 变量，禁止硬编码
- **禁止行内样式**：除动态计算值（如 `width: ${percent}%`）外
