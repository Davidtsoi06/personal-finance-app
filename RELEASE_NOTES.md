# 个人理财投资软件 v1.10.1 更新说明

> 发布日期：2026-08-16  
> 安装包：`personal-finance-setup-1.10.1.exe`

---

## 🏦 港银日结单支持（支出/收入分列 + 结余）

针对香港银行（如汇丰）日结单做了专门优化——这类单子**没有收支方向列**，而是「支出一列、收入一列、结余一列」：

- **AI 生成模板**：粘贴样例或**直接上传 Excel/CSV 文件**，AI 自动识别收入/支出分列，方向自动判断，无需手动设置
- **手动格式**：列映射新增「收入金额 / 支出金额」选项，结余列可映射为余额或忽略
- **修复自定义格式失效的隐藏问题**：自定义银行格式此前从未真正生效过（列映射解析 bug），现已修复——已有格式重新保存一次即可
- **日期解析增强**：支持 `18/08/2026`（日/月/年）自动回退识别；支持中文日期（`2026年8月18日`、`10月8日` 等，歧义由当前日期智能消解）
- **定期自动识别**：`NEW TERM DEP`（转定期自动创建）、`TERM DEP W\D` / `MATURITY`（回款自动结清）等英文关键词已支持

## 🎯 导入确认目标账户/卡号

- 导入日结单时新增「导入到账户」选择器：显示银行名 + 卡号尾号，**确认后导入**，切换账户时定期配对自动重算

## 🔄 总资产走势随价格更新

- 此前净值只在应用启动时记录一次，走势图不随当日行情变化；现在**每次股价或汇率更新都会自动更新当天净值**，走势图最新点即时反映最新市值

## 🔍 股票查询与分析

- 总览面板改名「股票查询与分析」，**只显示现有持仓**（持仓为 0 的股票不再显示）

## ⚙️ 版本信息

| 项目 | 内容 |
|------|------|
| 版本号 | `1.10.0` → `1.10.1` |
| 测试 | 197 项自动化测试全部通过 |
| 上一版回顾 | v1.10.0：卖出收益明细 + 自动刷新 + AI 生成模板 + 成本重放修复 |
| 下一版规划 | v1.11.0 效率工具：Ctrl+K 命令面板、图表实例复用、券商表单合并、AI 演示体验；定期派息登记（后续） |
