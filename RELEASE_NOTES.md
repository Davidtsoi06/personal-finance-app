# 个人理财投资软件 v1.9.1 更新说明

> 发布日期：2026-08-16  
> 安装包：`personal-finance-setup-1.9.1.exe`

---

## 📊 成本价一次性重算（修正历史漂移）

由于软件经过多次版本迭代，持仓成本价在加权计算过程中出现了累计漂移。本次更新在启动时**自动按你的历史交易记录重放计算一次**：

- 按时间顺序重放每一笔买入/卖出，重新算出正确的**成本价、持有数量、总成本、市值、盈亏**并更新
- 有拆分/分红/手工调整的资产：保留现有数量，只刷新成本价
- 没有交易记录的资产（手工录入）完全不动
- 重算前的旧值自动存档，需要时可对照

## 📋 投资收益明细新增三列

报表分析 → 投资收益明细新增：**成本价、交易价（现价）、成交数量**。

## 📦 数据包导入修复（重要）

修复「导出数据包 → 导入后启动报 database disk image is malformed」的问题：

- **导出**：改用自洽快照（数据库最新数据完整包含，不再缺数据）
- **导入**：自动备份现有数据 → 清理残留的临时数据库文件 → 校验文件合法性 → 替换 → 失败自动回滚
- 无效的数据包会被直接拒绝，不会覆盖你的数据

## ⚙️ 版本信息

| 项目 | 内容 |
|------|------|
| 版本号 | `1.9.0` → `1.9.1` |
| 测试 | 167 项自动化测试全部通过 |
| 数据迁移 | v21：成本价历史重放修复（自动执行，旧值留痕） |
| 上一版回顾 | v1.9.0：定期存款全自动体系（日结单驱动）+ 每日报表成本价 |
| 下一版规划 | v1.10.0 效率工具：Ctrl+K 命令面板、图表实例复用、券商表单合并、AI 演示体验；定期派息登记（后续） |
