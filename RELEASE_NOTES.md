# 个人理财投资软件 v1.6.1 更新说明

> 发布日期：2026-08-16  
> 安装包：`personal-finance-setup-1.6.1.exe`

---

## 💎 总资产口径修复（重要）

- **银行组内嵌券商漏计修复**：总资产卡 / 投资市值卡 / 净资产快照此前均少算银行组内嵌券商的持仓市值（演示数据实测少 ¥225,400），现全部正确
- **单一口径源**：新增 `computeAssetTotals` 纯函数，资产总览、净资产记录共用，杜绝两处口径漂移
- **净值历史窗口修复**：走势图原取「最早 30 天」，现取「最近 30 天」
- **汇率刷新后页面自动更新**：自动抓取实时汇率后，总览/资产管理/投资管理三页自动刷新，同一时刻各页数字一致（修复总览 713,350 vs 资产管理 676,570 的分叉问题）

## 🔗 资产间联动全部询问式（本次核心）

所有跨资产调整均经用户确认才执行，并自动写存取记录：

- **定期存款**：创建扣款同时写存取记录；编辑金额/币种弹窗询问「同步调整余额并写记录」或「仅改记录（转纯记录型）」；删除询问「退回金额并写记录」或「仅删除记录」
- **定期存款到期回款（新功能）**：到期后列表出现「💰 到期处理」——回款金额默认本金+利息（可改）、可选回款账户，确认后写存款记录并标记 ✅ 已结算（保留历史）
- **存取记录**：编辑带券商联动的记录时询问是否同步调整券商流动金（拒绝则脱钩）；删除弹窗明示将同步扣回券商流动金
- **保费缴纳**：明确「选账户即扣款并写记录 / 不指定仅记录不动余额」

## 🐛 其他修复

- **银行账户取出报错修复**：「转入投资账户」默认空值导致取出失败的校验问题（存入/取出均正常）

---

## 🔧 技术细节

| 项目 | 内容 |
|------|------|
| 版本号 | `1.6.0` → `1.6.1` |
| 数据库变更 | 迁移 v16 基础上新增 v17：`fixed_deposits.status`（active/settled，存量自动回填） |
| 测试 | 79 项自动化测试全部通过（新增定存联动/到期回款/资产口径等用例）；IPC 白名单 146 频道三处一致 |
| 诊断工具 | `scripts/inspect-summary.js`（只读总览口径打印）、`scripts/verify-demo-totals.js`（端到端回归） |