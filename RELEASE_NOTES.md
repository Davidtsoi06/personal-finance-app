# 个人理财投资软件 v1.10.17 更新说明

> 发布日期：2026-09-03
> 安装包：`personal-finance-setup-1.10.17.exe`

---

## 🤖 AI 持仓快照导出修复（快照文件从不生成的根因）

**问题**：设置中选择了 AI 分析软件数据文件夹，但 `portfolio_snapshot.json` 从未生成——AI 投资分析软件（v1.0.5 起）因此显示持仓消失。

**根因**：v1.10.15 引入的版本号读取用了相对路径 require，在打包产物目录结构中解析失败（少回溯一级），异常被静默吞掉，导致每次导出都在写文件前退出——不是没触发、不是设置没存、不是目录不存在。

**修复**：

- 版本号改为 **Electron 应用版本**读取（开发/打包一致），带多重兜底，**导出不再中断**
- 目标目录不存在时**自动创建**
- 导出失败不再静默：错误记录在案，设置页会显示「⚠️ 上次自动导出失败：原因」

修复后：选择文件夹的瞬间即会导出一份快照；之后任何持仓变化（交易/编辑/价格刷新）自动更新文件，AI 分析软件即可读取全部持仓数据。

## ⚙️ 版本信息

| 项目 | 内容 |
|------|------|
| 版本号 | 1.10.16 → 1.10.17 |
| 测试 | 264 项自动化测试全部通过（IPC 194 频道） |
| 上一版回顾 | v1.10.16：银行内嵌券商逻辑修复 |
| 下一版规划 | v1.11.1 批 C 效率工具 + AI 账目体检（暂缓，留待以后） |