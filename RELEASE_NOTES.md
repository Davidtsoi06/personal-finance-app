# 个人理财投资软件 v1.9.0 更新说明

> 发布日期：2026-08-16  
> 安装包：`personal-finance-setup-1.9.0.exe`

---

## 🏦 定期存款全自动体系（日结单驱动）

这是本次最大的变化：**定期存款不再需要手动创建和手动结清**——全靠银行日结单/月结单搞定，手动操作仍然保留作为兜底。

### 自动创建与自动结清

- 导入银行日结单时，每行自动分类：**转定期 / 定期回款 / 普通收支**（方向 + 摘要关键词识别）
- 「-10000 转定期」→ 自动创建一笔定期（来源「📄 日结单」，到期日待定），总资产不变
- 「+10300 到期回款」→ 自动匹配对应定期并**自动结清**：到期日自动补全，利息 = 回款 − 本金自动拆分，记入收支账本「投资收入」
- 总资产只增加利息——本金全程不算收入也不算支出

### 彻底告别重复记录

- 已手动创建的定期，导入日结单时自动**配对跳过**，不会重复扣款
- 先导入日结单、后手动创建时，弹窗建议「关联流水而非重复新建」
- 同一份日结单重复导入会被**指纹自动跳过**（防止双倍计入）
- 预览表格新增「分类 / 动作」列，每一行你都可以改主意

### 记录更清晰、纠错更安全

- 存取记录打标：🔁 转定期 / 💰 定期回款徽章
- 删除定期或流水时改为**联动询问**：一起删 / 仅删一侧 / 脱钩保留，不再一刀切禁止
- 定期卡片显示来源徽章、到期日待定提示、已结利息

## 📊 每日交易报表新增成本价

- 新增「成本价」列：卖出显示当日(含)之前的**买入加权平均成本**（后续买卖不会稀释历史盈亏）；买入显示本笔含费均价
- 无成本价的卖出，盈亏显示「—」并明确提示不参与汇总（修复了此前的 NaN 显示问题）
- 导出 Excel 同步新增成本价列

## ⚙️ 版本信息

| 项目 | 内容 |
|------|------|
| 版本号 | `1.8.3` → `1.9.0` |
| 测试 | 150 项自动化测试全部通过 |
| 数据迁移 | v20：定存流水表 + 内部转账打标 + 防重复指纹（旧数据自动回填） |
| 下一版规划 | v1.10.0 效率工具：Ctrl+K 命令面板、图表实例复用、券商表单合并、AI 演示体验；定期派息登记（后续） |
